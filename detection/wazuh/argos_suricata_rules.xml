<!-- ARGOS Suricata Rules - Wazuh XML -->
<!-- Eleva nivel de alertas Suricata ARGOS según gravedad real del ataque -->

<group name="argos,suricata,">

  <!-- SURICATA-ESC01: Reconocimiento activo TCP SYN Port Scan Nmap -->
  <rule id="110001" level="10">
    <if_sid>86601</if_sid>
    <field name="alert.signature">ARGOS RECON TCP SYN Port Scan - Nmap Invariant win1024</field>
    <description>ARGOS SURICATA-ESC01: Reconocimiento activo de red, TCP SYN Port Scan nmap invariant (T1046)</description>
    <mitre>
      <id>T1046</id>
    </mitre>
    <group>reconnaissance,network_scan,</group>
  </rule>

  <!-- SURICATA-ESC01b: Reconocimiento activo Generic Scanner Threshold -->
  <rule id="110002" level="10">
    <if_sid>86601</if_sid>
    <field name="alert.signature">ARGOS RECON TCP SYN Port Scan - Generic Scanner Threshold</field>
    <description>ARGOS SURICATA-ESC01b: Reconocimiento activo de red, Generic Scanner Threshold (T1046)</description>
    <mitre>
      <id>T1046</id>
    </mitre>
    <group>reconnaissance,network_scan,</group>
  </rule>

  <!-- SURICATA-ESC02: Brute Force SSH Non-Standard Banner -->
  <rule id="110003" level="12">
    <if_sid>86601</if_sid>
    <field name="alert.signature">ARGOS INITIAL ACCESS SSH Brute Force Non-Standard Client Banner</field>
    <description>ARGOS SURICATA-ESC02: Brute force SSH con cliente no estandar detectado, herramienta de ataque identificada (T1110.001)</description>
    <mitre>
      <id>T1110.001</id>
    </mitre>
    <group>initial_access,brute_force,</group>
  </rule>

  <!-- SURICATA-ESC03: Reverse Shell TCP hacia zona atacantes interna -->
  <rule id="110004" level="13">
    <if_sid>86601</if_sid>
    <field name="alert.signature">ARGOS C2 Reverse Shell TCP hacia zona atacantes interna</field>
    <description>ARGOS SURICATA-ESC03: Reverse shell TCP detectado desde endpoint hacia zona atacantes interna - C2 activo (T1059.004)</description>
    <mitre>
      <id>T1059.004</id>
    </mitre>
    <group>c2,reverse_shell,</group>
  </rule>

  <!-- SURICATA-ESC03b: Reverse Shell TCP hacia IP externa -->
  <rule id="110005" level="13">
    <if_sid>86601</if_sid>
    <field name="alert.signature">ARGOS C2 Reverse Shell TCP hacia IP externa puerto no estandar</field>
    <description>ARGOS SURICATA-ESC03b: Reverse shell TCP detectado desde endpoint hacia IP externa en puerto no estandar - C2 externo activo (T1059.004)</description>
    <mitre>
      <id>T1059.004</id>
    </mitre>
    <group>c2,reverse_shell,</group>
  </rule>

  <!-- SURICATA-ESC04: HTTP Beaconing en SOC LAN -->
  <rule id="110006" level="12">
    <if_sid>86601</if_sid>
    <field name="alert.signature_id">9000006</field>
    <description>ARGOS SURICATA-ESC04: Trafico HTTP anomalo detectado en SOC LAN, posible canal C2 via HTTP (T1071.001)</description>
    <mitre>
      <id>T1071.001</id>
    </mitre>
    <group>c2,beaconing,</group>
  </rule>

  <!-- SURICATA-ESC05: DNS Tunneling subdominio largo -->
  <rule id="110007" level="12">
    <if_sid>86601</if_sid>
    <field name="alert.signature_id">9000007</field>
    <description>ARGOS SURICATA-ESC05: DNS Tunneling detectado, subdominio de mas de 30 caracteres, posible canal C2 via DNS (T1071.004)</description>
    <mitre>
      <id>T1071.004</id>
    </mitre>
    <group>c2,dns_tunneling,</group>
  </rule>

</group>
